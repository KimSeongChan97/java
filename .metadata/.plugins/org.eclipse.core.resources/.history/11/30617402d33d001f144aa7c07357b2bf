package array;

import java.util.Arrays;
import java.util.HashSet;
import java.util.Random;
import java.util.Scanner;
import java.util.Set;

public class LottoSolo {

    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        Random random = new Random();

        System.out.print("돈 입력 : ");
        int money = scan.nextInt();
        int linesToGenerate = money / 1000; // 입력된 돈으로 생성할 줄 수 계산

        for (int line = 0; line < linesToGenerate; line++) {
            int[] lottoNumbers = generateLottoNumbers(random); // 로또 번호 생성
            printFormattedNumbers(lottoNumbers); // 포맷에 맞게 출력

            // 5줄마다 줄바꿈
            if ((line + 1) % 5 == 0 && line != linesToGenerate - 1) {
                System.out.println();
            }
        }

        scan.close();
    }

    // 중복되지 않는 로또 번호 생성 및 정렬
    public static int[] generateLottoNumbers(Random random) {
        int[] lottoNumbers = new int[6];
        Set<Integer> generated = new HashSet<>();

        for (int i = 0; i < 6; i++) {
            int num;
            do {
                num = random.nextInt(45) + 1; // 1부터 45까지의 난수 생성
            } while (generated.contains(num)); // 이미 생성된 번호인지 확인

            lottoNumbers[i] = num;
            generated.add(num);
        }

        Arrays.sort(lottoNumbers); // 번호 오름차순 정렬

        return lottoNumbers;
    }

    // 포맷에 맞게 번호 출력
    public static void printFormattedNumbers(int[] numbers) {
        for (int i = 0; i < numbers.length; i++) {
            System.out.printf("%5d", numbers[i]);
        }
        System.out.println();
    }
}



/*
[문제] 로또 프로그램 - 자동 번호
1. 1~45까지의 정수형 값을 가진 6개인 배열이다.
2. 중복된 숫자를 가지면 안된다.
3. 오름차순으로 정렬한다.
4. 1줄당 1000원이다.
5. 출력시 5자리로 맞춘다.
6. 5줄마다 줄바꿈을 한다.

[실행결과]
돈 입력 : 3200
    2    4   19   39   43   44
   22   26   33   38   39   42
    5    6    8   25   35   45
    
현금 입력 : 7000    
    2    4   19   39   43   44
   22   26   33   38   39   42
    5    6    8   25   35   45
    3   14   23   30   34   35
   18   20   25   27   32   37

    1   16   32   34   41   42
    5    6   18   30   33   44


*/