package homework;

import java.util.Scanner;

public class SungjukOXSolo {
	static final String JUNG = "11111"; // 필드 - 상수화(불변값)를 알려주기 위해 대문자 사용
	
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		
		// 인원수 입력
		System.out.print("인원수 입력 : ");
		int numOfStudents = scanner.nextInt();
		scanner.nextLine(); // 버퍼 비우기
		
		// 배열 초기화
		String[] name = new String[numOfStudents];
		String[] dap = new String[numOfStudents];
		char[][] ox = new char[numOfStudents][5];
		int[] score = new int[numOfStudents];
		
		// 데이터 입력
		for (int i = 0; i < numOfStudents; i++) {
			System.out.print("이름 입력 : ");
			name[i] = scanner.nextLine();
			
			System.out.print("답안지 입력 : ");
			dap[i] = scanner.nextLine();
		}
		
		// 성적 처리
		for (int i = 0; i < numOfStudents; i++) {
			for (int j = 0; j < 5; j++) {
				if (dap[i].charAt(j) == JUNG.charAt(j)) {
					ox[i][j] = 'O';
					score[i] += 20;
				} else {
					ox[i][j] = 'X';
				}
			}
		}
		
		// 결과 출력
		System.out.println("\n*** 성적표 ***");
		System.out.println("이름      1 2 3 4 5   점수");
		for (int i = 0; i < numOfStudents; i++) {
			System.out.print(name[i] + " ");
			for (int j = 0; j < 5; j++) {
				System.out.print(" " + ox[i][j]);
			}
			System.out.println("   " + score[i]);
		}
		
		scanner.close();
	}
}
/*

[문제] 성적처리 프로그램
- 5문제는 사지선다형 문제이다
- 정답은 "11111"
  static final String jung = "11111"; //상수화
- 1문제당 20점씩 한다.
  
   배열기준
String[] name = new String[2];
String dap;
char[][] ox = new char[2][5];
int score = new int[2];

[실행결과]
인원수 입력 : 2

이름 입력 : 홍길동
답안지 입력 : 12311

이름 입력 : 코난
답안지 입력 : 11311

      *** 성적표 ***
이름      1 2 3 4 5   점수
홍길동     O X X O O   60
코난      O O X O O   80


*/